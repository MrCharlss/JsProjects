import React, { Component } from "react";

const Controles = () => {
  return (
    <div>
      <button>Start</button>
      <button>Stop</button>
      <button>Reset</button>
    </div>
  );
};

export default Controles;
